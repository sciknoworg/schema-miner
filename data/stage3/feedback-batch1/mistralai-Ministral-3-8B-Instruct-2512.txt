Method 1

1. topology.metal_centers and composition.metal_ions overlap → merge into a single metal_centers definition. composition.ligands and reactants.ligand_sources should be linked (define once, reference in synthesis). 2. Stoichiometry: unify into one structured numeric object (avoid "10:8" strings). Conditions: split into environmental (T, P) vs operational (stirring, time). 3. n/a 4. descriptors could e more formal(i.e. not 'friendly')
