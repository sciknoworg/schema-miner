FEEDBACK:
The schema shows good improvement. Catalyst characterization is now correctly nested inside the catalyst object. Feedstock contaminants (chlorine_ppm, heavy_metals), particle_size, and moisture_content are now present. Product quality has been expanded (HHV, viscosity, density, chlorine_ppm). The analytical_methods enum has been significantly expanded with GCxGC-FID/MS, BET, NH3-TPD, SEM, bomb calorimetry, and ASTM fuel tests.

Property Merging: No merging needed.

Property Grouping: Nesting catalyst characterization inside the catalyst object (as requested in Batch 1) is now correctly implemented — this is the most important structural improvement in this batch.

Missing Properties — PREVIOUSLY REQUESTED AND STILL NOT INCORPORATED: (1) Char composition (carbon_content_pct, ash_content_pct) was requested in Batch 1 and is still absent from product_composition — consistently reported in all batch papers. (2) Making metadata fields required (at least author and doi) was requested in Batch 1 — none of the metadata fields are required. A valid instance can be submitted with no metadata at all. (3) Making feedstock.preparation optional was requested in Batch 1 — it is still required, which will cause valid Stage 3 papers that do not report preprocessing to fail validation. (4) NH3-TPD was requested to be added to the analytical_methods enum in Batch 1 — it is now present. Resolved.

Missing Properties — NEW: (5) No char sub-object in product_composition alongside liquid and gas arrays — only liquid and gas are present; char compounds are absent.

Adequacy of Descriptions: Clear with good constraint notes. Product quality correctly includes all four key metrics (HHV, viscosity, density, chlorine_ppm).

JSON SNIPPETS:
Add char composition to product_composition (previously requested):
```json
"char": {
    "type": "object",
    "description": "Properties of the solid residue (char).",
    "properties": {
        "carbon_content_pct": {"type": "number", "minimum": 0, "maximum": 100, "description": "Fixed carbon content (wt%)"},
        "ash_content_pct": {"type": "number", "minimum": 0, "maximum": 100, "description": "Ash content (wt%)"},
        "chlorine_ppm": {"type": "number", "minimum": 0, "description": "Chlorine content in char (ppm)"}
    }
}
```

Make metadata partially required (previously requested):
```json
"metadata": {
    "type": "object",
    "required": ["author", "doi"],
    "properties": {
        "study_id": {"type": "string"},
        "doi": {"type": "string"},
        "author": {"type": "string"},
        "publication_year": {"type": "integer", "minimum": 1990, "maximum": 2030}
    }
}
```

Make feedstock preparation optional (previously requested):
```json
"feedstock": {
    "required": ["types", "composition"]
}
```

ADDITIONAL COMMENTS:
Good structural progress in this batch - catalyst characterization nesting is correctly resolved. Three items from Batch 1 remain: char composition, metadata requirements, and feedstock preparation optionality. All three are straightforward and must be addressed in Stage 3. Making feedstock.preparation optional is particularly important given the larger Stage 3 corpus where many papers will not report preprocessing details.
